import Enum from "emcjs/data/Enum.js";

export default class FixedAnchor extends Enum {

    static NONE = new this("none");

    static START = new this("start");

    static END = new this("end");

}
