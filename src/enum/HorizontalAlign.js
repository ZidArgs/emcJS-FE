import Enum from "@emcjs/core/enum/Enum.js";

export default class HorizontalAlign extends Enum {

    static TOP = new this("left");

    static CENTER = new this("center");

    static BOTTOM = new this("right");

}
