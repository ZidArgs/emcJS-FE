import Enum from "emcjs/data/Enum.js";

export default class ButtonBorderPositions extends Enum {

    static ALL = new this("all");

    static LEFT = new this("left");

    static RIGHT = new this("right");

    static TOP = new this("top");

    static BOTTOM = new this("bottom");

}
